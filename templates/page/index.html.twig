{% extends 'base.html.twig' %}
{% block body %}
	{% set user = app.getUser() %}

	{% if user is not null %}
		<p>Bienvenue,
			{{ user.firstname }}
			!</p>
	{% else %}
		<p>Vous n'êtes pas connecté.</p>
	{% endif %}

{% endblock %}
